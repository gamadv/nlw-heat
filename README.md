TS start with: yarn tsc --init
Github OAuth App created

Prisma:
- Create migrates: yarn prisma migrate dev

Migration: DB history

Protocolo Websocket:
HTTP == requisição com inicio e fim
Websocket == realtime communication